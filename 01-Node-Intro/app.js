//! ****************************************** ACCESSING APPLICATIONS ******************************************
const { exec } = require("child_process");

// exec("google-chrome");
// exec("firefox");
// exec("code");

// exec("/usr/bin/google-chrome");

// exec("/snap/bin/whatsdesk");
// exec("/snap/bin/telegram-desktop");

exec("node");
